********************************************************************
4 Bit Comparator
********************************************************************


module comparator_4_df2 (gr, eq, sm, a, b);

input [3:0] a,b;
output gr, eq, sm;

assign gr = (a > b);

assign eq = (a==b);

assign sm = (a < b);

endmodule

